# Branding
NEXT_PUBLIC_AGENT_NAME=Agent
NEXT_PUBLIC_AGENT_TAGLINE=Your AI assistant

# OpenClaw gateway connection (used by openclaw CLI)
OPENCLAW_GATEWAY_URL=http://127.0.0.1:18789
OPENCLAW_GATEWAY_TOKEN=replace-me

# Optional: override workspace root used by memory/activity routes
# Defaults to: $HOME/.openclaw/workspace
# OPENCLAW_WORKSPACE=/home/your-user/.openclaw/workspace
